TODO
- Wireframe mode
  To draw your triangles in wireframe mode, you can configure how OpenGL draws its primitives via glPolygonMode(GL_FRONT_AND_BACK, GL_LINE).
  The first argument says we want to apply it to the front and back of all triangles and the second line tells us to draw them as lines.
  Any subsequent drawing calls will render the triangles in wireframe mode until we set it back to its default using glPolygonMode(GL_FRONT_AND_BACK, GL_FILL).
- Camera
- Proper resizing
- PicoEngine namespace
- move stuff to the stack

- Batch Rendering (DONE)
- Replace all Renderer with Renderer2D (DONE)
- Logger (DONE)
- Replace GLCall with glEnableDebug() (DONE)
